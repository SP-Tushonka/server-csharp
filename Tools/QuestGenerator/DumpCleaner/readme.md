## **SPT Dump cleaner**
Put raw dump files e.g. `resp.client.items_xxxxx.json` in Input folder

run `DumpCleaner.exe`

Take outputted files from `/output` and place in `server/project/assets/database`